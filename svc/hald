#!/bin/sh
. /etc/leaninit/rc.svc

# HALD
NAME="HALD"

main() {
	waitfor service dbus
	hald --daemon=no --use-syslog &
	jobs -p %+ > "$__svcpidfile"
}

# Kill all hald related processes
stop() {
	pkill hald
}

run "$@"
