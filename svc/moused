# Service for FreeBSD's moused
NAME="moused"
DAEMON=moused

# moused is extremely stubborn, this just discards it should it fail to work
main() {
	waitforinit devd moused 'kill -s SIGKILL `pgrep -x moused`' $1
	pkill -KILL -x moused
}
