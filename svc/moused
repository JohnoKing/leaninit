NAME="moused"
DAEMON="moused"

main() {
	waitforinit devd moused 'kill -s SIGKILL `pgrep -x moused`' $1
	waitforinit hald hald command $1
	sleep 5
	pkill -KILL moused
	sleep 5
	moused -f -p /dev/${MOUSE}
}
