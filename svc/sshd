# Secure SHell Daemon
NAME="SSH"
DAEMON=sshd

main() {
	# Wait for internet services
	waitforinit wpa_supplicant wpa_supplicant true $1
	waitforinit dhcpcd dhcpcd true $1
	sleep 1

	# sshd must be executed with the full path to the executable
	command `command -v sshd`
}
