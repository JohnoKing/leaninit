#!/bin/sh
#
# rc.banner - This script will be run before rc(8), allowing the user
# to display a custom banner on system startup.
#
# The first example runs neofetch without color blocks enabled.
#
# The second example uses banner from AT&T AST with lolcat to
# display the kernel name with a rainbow effect.
#

# Example 1:
exec neofetch --color_blocks off

# Example 2:
#banner -d '-/' "$(uname)" | lolcat
