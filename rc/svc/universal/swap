#!/bin/sh
NAME="swap"
MSG="Turning on all swap partitions"

main()
{
#DEF FreeBSD
    swapon -aL
#ENDEF
#DEF Linux
    swapon -a
#ENDEF
}

stop()
{
#DEF FreeBSD
    swapon -aL
#ENDEF
#DEF Linux
    swapon -a
#ENDEF
}

. /etc/leaninit/rc.svc
